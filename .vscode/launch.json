{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Django",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/manage.py",
      "args": [
        "runserver",
        "--noreload" // Optional: to prevent issues with hot reloading
      ],
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
